---
title: 'List Batch Jobs'
description: 'List all batch jobs for your organization with optional status filter and pagination.'
openapi: 'GET /batch-lookups'
---

## Overview

List batch jobs with optional filtering by status (`pending`, `processing`, `completed`, `failed`, `cancelled`) and pagination via `limit` and `offset`.

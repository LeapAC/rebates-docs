---
title: 'Search Applications'
description: 'Search and filter applications with pagination. Returns a list of applications matching the specified criteria.'
openapi: 'GET /application/applications'
---

<Note>
This endpoint requires API key authentication via the `x-api-key` header or `Authorization: Bearer <API key>`.
</Note>

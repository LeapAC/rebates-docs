{
  "specsDir": "api-reference/specs",
  "repos": {
    "LeapAC/global-connect-service": { "localEnv": "GCS_REPO", "localDefault": "/Users/sean/Code/global-connect-service" },
    "LeapAC/incentives-service": { "localEnv": "INCENTIVES_REPO", "localDefault": "/Users/sean/Code/incentives-service" }
  },
  "gcsServers": [
    { "url": "https://api.leap.energy", "description": "Production" },
    { "url": "https://api.staging.leap.energy", "description": "Staging" }
  ],
  "specs": [
    {
      "output": "gcs-incentives.json",
      "title": "Leap Incentives — Lookups",
      "serversRef": "gcsServers",
      "security": "bearer",
      "tag": "incentives-lookups",
      "source": { "repo": "LeapAC/global-connect-service", "entry": "src/main/resources/openapi/incentives.yaml", "deps": ["src/main/resources/openapi/common-schemas.yaml"] },
      "include": [
        { "method": "post", "path": "/beta/incentives/lookups", "title": "Look up incentives", "description": "Check which rebate programs a customer qualifies for. Send the customer's address, classification, and devices; Leap returns the total incentive amount, a per-program breakdown, and a Connect URL the customer can use to apply." },
        { "method": "post", "path": "/beta/incentives/refreshes", "title": "Refresh incentives", "description": "Re-run eligibility for a customer you've already looked up, using their stored address and devices — no need to resend them. Use this to pick up newly launched programs or updated incentive amounts." },
        { "method": "post", "path": "/beta/incentives/overrides", "title": "Override incentives", "description": "Replace an existing customer's device list, then re-run eligibility. Use this when a customer's equipment has changed. The previous device set is discarded." }
      ],
      "schemaDescriptions": {
        "EligibilityRequest.create_application": "Create the Connect application records for this lookup's eligible programs, so the returned `connect_url` is a link the customer can open. Provisioning runs in the background and is normally in place within seconds. Defaults to `false`: lookup-only integrations are unaffected and no applications are created for them. No effect on billing or rate limits.",
        "EligibilityRequest.include_preview": "Include your own unpublished partner offer in the results so you can test it before publishing. Only your own offer is ever included. Utility, state, and market programs are unaffected. Defaults to `false`.",
        "EligibilityResult.connect_url": "Deep link to hand the customer so they can apply: `https://connect.incentives.leap.energy/{company}/refId/{reference_id}`, where `{company}` is your registration nickname. **Omitted entirely when the lookup surfaces no eligible incentive** — there is nothing to apply for, so check that the field is present before showing a link.",
        "IncentiveSummary.ongoing_amount": "Total recurring incentives across all eligible programs, **per year**.",
        "IncentiveSummary.upfront_amount": "Reserved for a payment kind Leap does not pay yet, so it is always 0. Read `install_amount` for the one-time total.",
        "IncentiveSummary.install_amount": "Total one-time incentives across all eligible programs, paid at install time.",
        "EligibleProgram.upfront_amount": "Reserved for a payment kind Leap does not pay yet, so it is always 0. Read `install_amount` for this program's one-time incentive.",
        "EligibleProgram.install_amount": "This program's one-time incentive, paid at install time.",
        "EligibleProgram.ongoing_amount": "This program's recurring incentive, **per year**.",
        "RefreshIncentivesRequest.eiaid": "Optional utility override, matching the one on **Look up incentives**. Supply an EIA utility id to re-resolve the customer's serving utility instead of reusing the one on file. Leap re-selects candidate programs against it, re-runs eligibility, and keeps the override for later calls on this customer.",
        "EligibleProgram.name": "Program name as shown to the customer. Leap sets this for utility, state, and market programs; for your own partner offer it is the offer name you configure in the portal.",
        "EligibleProgram.program_identifier": "Stable identifier for this program — your offer's identifier for a partner offer, or the catalog identifier (for example `PGE-ART`) otherwise. Use it to correlate the same program across lookups.",
        "EligibleProgram.is_partner_offer": "`true` when this entry is your own partner offer (which bundles one or more underlying programs); `false` for utility, state, and market rebate programs. Use it to separate your offer from rebates in your own UI.",
        "MatchedTier.incentive_amount": "Tier total across the devices that passed this tier's requirements; per-device outcomes are in `device_results[]`. Units follow `payment_type`: `UPFRONT` and `INSTALL` amounts are one-time, `ONGOING` amounts are per year.",
        "PartnerBranding": "Your brand name and logo as configured on your Leap account, so a surface rendering these results can show your branding without a second call.",
        "CustomerDeviceRef": "One installation. New installations identify their catalog device with exactly one of `device_id` / `device_source_id`. On **Refresh incentives**, `customer_device_id` instead targets an installation already on file, for an in-place update.",
        "CustomerDeviceRef.device_source_id": "Alternative way to address the catalog device: its numeric catalog `source_id`. Unknown values are rejected with 422 `INVALID_DEVICE_ID`. Supply exactly one of `device_id` / `device_source_id` on new installations.",
        "CustomerDeviceRef.customer_device_id": "Identifies one installation — this customer, this catalog device, your device reference. Leap assigns it on the first lookup and echoes it back in `customer_devices[]`; it is stable across later lookups and refreshes, and is regenerated only by **Override incentives**, which replaces the device set. Send it on **Refresh incentives** to update that installation in place; it is rejected with 400 on lookups and overrides.",
        "RefreshIncentivesRequest.customer_devices": "Optional device merge set. An entry carrying a `customer_device_id` updates that installation in place; an entry without one adds a new installation. Installations already on file that you don't list are left untouched — sending a subset never deletes the rest.",
        "RefreshIncentivesRequest.remove_customer_device_ids": "Remove installations by `customer_device_id`. Ids that don't match a device on this customer are ignored, so retries are safe.",
        "RefreshIncentivesRequest.remove_device_references": "Remove installations by `partner_device_reference`. Every installation with a matching reference is removed; unknown references are ignored.",
        "PartnerBranding.logo_url": "URL to your logo, as configured on your Leap account. Null when no logo is configured.",
        "EligibilityCheck.source_program_identifier": "On a partner offer, the program this check came from — your offer's identifier for checks on the offer itself, or the identifier of an underlying program for checks it inherits. Null on rebate programs.",
        "EligibilityRequest.customer_details": "Attributes you're supplying for the customer, keyed by requirement key (for example `customer.email`, `customer.is_homeowner`). They're stored against the customer and used when eligibility is evaluated. Omit or leave empty if you have none. Maximum 100 entries.",
        "EligibleProgram.included_leap_program_ids": "Partner offers only: identifiers of the underlying programs this offer covers for this customer. Empty for every other kind of entry.",
        "CustomerClassification": "The customer or site classification used to determine eligibility. `RESIDENTIAL` and `COMMERCIAL` are the long-standing values; `MULTIFAMILY` and `MANUFACTURED_HOME` exist because rebate qualification differs across single-family, multifamily, and manufactured housing.",
        "CustomerDeviceRefEcho.customer_device_id": "Leap's identifier for this installation. Store it — it's how you address that specific device on later calls.",
        "EligibilityRequest.reference_id": "Your stable identifier for this customer. Leap uses it to recognise a returning customer, and you'll send it again on **Refresh incentives**, **Override incentives**, and when searching applications. It's scoped to you — another partner using the same value is a different customer.",
        "EligibilityRequest.eiaid": "Optional utility override. Supply it when you already know the customer's serving utility and the address-based match is unavailable or ambiguous: Leap uses this EIA utility id to select candidate programs instead of resolving the utility from the address. On this path the address isn't geocoded, so the response omits the `geocoding` block and any stored coordinates are left as they are. A value matching no utility fails the same way an unserved address does (404 `NO_UTILITY_AT_ADDRESS`)."
      }
    },
    {
      "output": "gcs-devices.json",
      "title": "Leap Incentives — Devices",
      "serversRef": "gcsServers",
      "security": "bearer",
      "tag": "devices",
      "source": { "repo": "LeapAC/global-connect-service", "entry": "src/main/resources/openapi/programs.yaml", "deps": ["src/main/resources/openapi/common-schemas.yaml"] },
      "include": [
        { "method": "get", "path": "/beta/incentives/devices/search", "title": "Search device catalog", "description": "Find the catalog `device_id` for a product by free-text search over manufacturer, model, and description. Use this to map your product catalog to Leap's programmatically instead of by hand, and to keep the mapping current as the catalog grows. Every whitespace-separated term in `q` must match; results are ranked by closeness." },
        { "method": "get", "path": "/beta/incentives/devices/saved", "title": "List saved devices", "description": "List the devices you've saved, most recently saved first. Each entry carries the full catalog record plus your own optional label. Saving the products you sell keeps them on hand without a catalog search every time. Results are scoped to your account; page through them with `limit` and `offset`." },
        { "method": "put", "path": "/beta/incentives/devices/saved/{device_id}", "title": "Save a device", "description": "Add a catalog device to your saved list, or relabel one you already saved. The `label` is your own name for the device and is optional: omit the key to keep the current label, send null or a blank string to clear it, send text to set it. Repeating the call is safe, because the same `device_id` updates the existing entry instead of adding a second one. Catalog devices that Leap has retired return 404." },
        { "method": "delete", "path": "/beta/incentives/devices/saved/{device_id}", "title": "Remove a saved device", "description": "Remove a device from your saved list. The catalog device is untouched, and so is any lookup that referenced it. Removing a device you have not saved also returns 204, so retries are safe." }
      ],
      "schemaDescriptions": {
        "DeviceResponse": "One device in Leap's catalog. Send its `id` as the `device_id` on a lookup. Specification fields such as `capacity_kw` or `seer2_rating` are populated where Leap holds data for that device type, and are null otherwise.",
        "SavedDeviceUpsertRequest.label": "Your own name for the device, to help you find it later. Omit the key to keep the current label, send null or a blank string to clear it, send text to set it. Maximum 256 characters.",
        "SavedDeviceResponse.device_id": "Catalog device UUID. This is the `device_id` you send on a lookup.",
        "SavedDeviceResponse.label": "Your own name for the device, when you set one. Null otherwise.",
        "SavedDeviceResponse.created_at": "When you first saved this device.",
        "SavedDevicePage.total": "Total devices on your saved list, ignoring paging."
      }
    },
    {
      "output": "incentives-applications.json",
      "title": "Leap Incentives — Applications",
      "servers": [
        { "url": "https://api.incentives.leap.energy/alpha", "description": "Production" },
        { "url": "https://api.staging.incentives.leap.energy/alpha", "description": "Staging" }
      ],
      "security": "apikey",
      "tag": "applications",
      "source": { "repo": "LeapAC/incentives-service", "entry": "supabase/functions/applications/openapi.json", "deps": [] },
      "replacements": [
        { "from": "https://your-project.supabase.co/storage/v1/object/sign/attachments/", "to": "https://<storage-host>/attachments/" },
        { "from": "https://your-project.supabase.co/storage/v1/object/public/attachments/", "to": "https://<storage-host>/attachments/" },
        { "from": "/uuid-invoice.pdf", "to": "/<uuid>-invoice.pdf" }
      ],
      "include": [
        { "method": "get", "path": "/applications", "title": "Search applications", "description": "Search and filter the applications you're tracking. Returns a paginated list, filterable by reference ID or status." },
        { "method": "get", "path": "/applications/{application_id}", "title": "Get application", "description": "Retrieve a single application in full, including customer information and device details." },
        { "method": "patch", "path": "/applications/{application_id}", "title": "Update application", "description": "Provide the information you're supplying on behalf of the customer — customer details, device data, purchase and installation dates, contractor info. Only include the fields you want to change; omitted fields keep their current values.\n\nStatus and system identifiers (`id`, `customer_id`, `program_id`) are read-only and can't be changed here." },
        { "method": "get", "path": "/applications/attachments", "title": "List attachments", "description": "List the attachments on one or more applications. Pass `application_id` (with an `attachment_type`) for a single application, or `application_ids` (comma-separated) for several." },
        { "method": "post", "path": "/applications/attachments", "title": "Upload attachment", "description": "Create a signed URL for uploading an attachment. Upload the file to that URL with a PUT request, then confirm it with the commit endpoint." },
        { "method": "post", "path": "/applications/attachments/commit", "title": "Commit attachment upload", "description": "Confirm an attachment after the file has been uploaded to its signed URL. This finalizes the attachment on the application." },
        { "method": "get", "path": "/applications/attachments/{id}/download", "title": "Download attachment", "description": "Get a signed URL for downloading an attachment. The URL is valid for one hour." },
        { "method": "delete", "path": "/applications/attachments/{id}", "title": "Delete attachment", "description": "Permanently delete an attachment. This removes the file and its record and can't be undone." }
      ]
    }
  ]
}
